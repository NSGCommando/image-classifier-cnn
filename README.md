# Image Classifier #
A CNN-based image classifier trained on MNIST dataset

## Architecture ##
- Structure:
 - Input layer with input shape of 28x28 pixels per image
 - Two blocks of Conv2d layers, blocks structured as follows: ```COnv2d -> BatchNorm -> ReLUActivation -> MaxPooling```
 - Conv2d blocks followed by two dense layers of 128 and 256 neurons each, second dense layer has L2 Regularization applied to it
 - Final layer is dense layer for classification with 10 output neurons and Softmax activation
 - Inference script has checks for white-background images, and will flip image arrays if it is detected that the image has high intensity backgrounds to help classify input images
 - Can be run in CLI mode locally as well as over HTTP via Uvicorn server

## Training ##
- To train the model and generate model weights, run the `training.py` script from project root - `python training.py`
- Model weights stored at `src/models/` as `.keras` files
- Model best accuracy stored at `src/results/eval_accuracy.txt`


## Inference ##
### Run Inference Server ###
- Run `Uvicorn` single-worker server via `python run.py` from project root and access the API at `localhost:8000`
- Test the API functioning for `/api/predict-one` via opening `localhost:8000/docs`; this is an autogenerated page using Swagger UI and OpenAPI specifications for API documentation

### Run CLI Inference ###
- To run CLI inference on a single image, use the following command in terminal from the root folder: ```cli.bat --image [image_path]```
- If running in a virtual environment, and Tensorflow is not installed globally, venv scripts should be enabled first.
  - If terminal is Windows PowerShell, it might be necessary to bypass PowerShell's script execution restrictions for current terminal session using ```Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass```
    - Followed by ```.\venv\Scripts\Activate.ps1``` to activate scripts then running inference command above

## Implemented ##
- Training
- Weight Loading and Saving
- Single-Image Inference
- Handle Byte-streams and FilePaths (done via strategy pattern dispatcher)

